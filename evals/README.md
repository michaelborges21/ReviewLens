# evals/

Golden sets, red-team set e runners de avaliação (spec `06-evals.md`).

Ainda não existe conteúdo aqui: golden set de Q&A, retrieval e red-team são trabalho humano
(rotulagem manual) descrito na spec, não estrutura de código. `make eval` / `make eval-smoke`
apontam para cá até isso ser criado.
